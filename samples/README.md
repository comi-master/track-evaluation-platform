# Synthetic sample tracks

These small CSV files are artificially constructed for demonstrations. They contain no real experiment, customer, classified, or personal data. `invalid-track.csv` intentionally has a malformed numeric value.
