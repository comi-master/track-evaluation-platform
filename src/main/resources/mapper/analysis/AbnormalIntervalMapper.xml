<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.trackanalysis.analysis.infrastructure.persistence.AbnormalIntervalMapper">
  <insert id="batchInsert">INSERT INTO abnormal_interval (analysis_result_id,interval_no,start_sequence,end_sequence,start_time,end_time,point_count,peak_error,peak_error_time,created_at) VALUES <foreach collection="intervals" item="i" separator=",">(#{i.analysisResultId},#{i.intervalNo},#{i.startSequence},#{i.endSequence},#{i.startTime},#{i.endTime},#{i.pointCount},#{i.peakError},#{i.peakErrorTime},#{i.createdAt})</foreach></insert>
</mapper>
