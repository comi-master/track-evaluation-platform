<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.trackanalysis.track.infrastructure.persistence.TrackPointMapper">
    <insert id="batchInsert">
        INSERT INTO track_point
        (track_file_id, sequence_no, time_value, true_x, true_y, true_z,
         track_x, track_y, track_z, created_at)
        VALUES
        <foreach collection="points" item="point" separator=",">
            (#{point.trackFileId}, #{point.sequenceNo}, #{point.timeValue},
             #{point.trueX}, #{point.trueY}, #{point.trueZ},
             #{point.trackX}, #{point.trackY}, #{point.trackZ}, #{point.createdAt})
        </foreach>
    </insert>
</mapper>
